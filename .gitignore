# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# Temporary files
*.tmp
*.bak
*.log

# SQLite runtime files
*.db-shm
*.db-wal

# SQLite database (user data — never overwrite via git merge)
shotpilot-app/data/*.db
shotpilot-app/*.db

# Uploaded user assets (do not commit uploads)
shotpilot-app/uploads/
# Compressed files (we want the source files, not zips)
*.zip
*.tar.gz
*.rar

# Environment files (API keys — NEVER commit)
.env
*.env.local
