import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

console.log("SHOTPILOT: Main script loading (v2 - cache busted)");
ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
